# Changelog

## Unreleased

- The legacy date arguments of `PasswordStatusInfo` are deprecated and will be
  removed in the next major version. New code must use their corresponding
  `*_at` arguments.
